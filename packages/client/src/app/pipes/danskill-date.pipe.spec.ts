import { DanskillDatePipe } from './danskill-date.pipe';

describe('DanskillDatePipe', () => {
  it('create an instance', () => {
    const pipe = new DanskillDatePipe();
    expect(pipe).toBeTruthy();
  });
});
