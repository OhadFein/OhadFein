{
  "private": true,
  "name": "@danskill/workspace",
  "version": "1.0.0",
  "main": "index.js",
  "workspaces": {
    "packages": ["packages/*"],
    "nohoist": ["**/@types/jasmine", "**/@types/jasmine/**"]
  },
  "scripts": {
    "dev": "lerna run dev --stream",
    "test": "lerna run test",
    "setup": "lerna bootstrap",
    "build": "lerna run build --parallel",
    "start": "yarn workspace webapp-server start"
  },
  "devDependencies": {
    "lerna": "4.0.0"
  }
}
